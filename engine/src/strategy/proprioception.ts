/**
 * DEPRECATED: Use narrative-prompt.ts instead.
 *
 * This module is preserved for backward compatibility.
 * The narrative prompt builder replaces proprioception + nudges + memories
 * with a single unified call that produces rich narrative prose.
 */

export { buildNarrativePrompt } from './narrative-prompt.js';
